<div class="custom-text-section">
    <h1> {{ section.settings.custom_text_title }} </h1>
</div>
  
<div class="block">
    {% for block in section.blocks %}
    {% if block.settings.custom_text_title != nil %}
    <div class="test">
        <h1 class="chiffre">{{ block.settings.custom_text_title }}</h1>
        <div class="texte">{{ block.settings.custom_text_body }}</div>    
    </div>
    {% endif %}
    {% endfor %}
</div>
  
  {% schema %}
    {
        "name": "Def",
        "settings": [
            {
                "id": "custom_text_title",
                "type": "text",
                "label": "Titre",
                "default": "Entrer le titre"
            }
        ],
        "blocks": [ 
            {
                "name": "text img",
                "type": "text",
                "settings": [
                    {
                        "id": "custom_text_title",
                        "type": "text",
                        "label": "Titre",
                        "default": "Entrer le titre"
                    },
                    {
                        "id": "custom_text_body",
                        "type": "richtext",
                        "label": "Texte",
                        "default": "<p>Entrer le texte</p>"
                    }
                 ]
            }
  
        ],
        "presets": [
            {
                "name": "Def",
                "category": "Custom"
            }
        ]
    }
  {% endschema %}